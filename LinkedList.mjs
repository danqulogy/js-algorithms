

export class LinkedList{
    list = []

    constructor(){
        /** @var LinkedListNode */
        this.head = null
        this.tail = null
    }

    prepend(value){
        const node = new LinkedListNode(value, this.head)

    
    }
}